{
  "name": "codepulse-platform",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@11.0.0",
  "description": "CodePulsePlataform - A modern learning and competition platform with microservices",
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "db:generate": "turbo run db:generate",
    "db:migrate": "turbo run db:migrate"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.0.0"
  }
}
